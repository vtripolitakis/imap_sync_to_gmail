# Example .env for imap_sync_to_gmail.py
# Copy this to .env and fill in the secrets before running.

# Source IMAP (server A)
SRC_IMAP_HOST=imap.source-server.com
SRC_IMAP_USER=user@source-server.com
SRC_IMAP_PASS=your-source-password
SRC_FOLDER=INBOX

# Gmail IMAP
GMAIL_IMAP_HOST=imap.gmail.com
GMAIL_USER=your-gmail@gmail.com
GMAIL_APP_PASS=your-16-char-app-password
# Gmail label (Gmail uses labels, not folders; use / for nested labels)
GMAIL_FOLDER=Imported/FromServerA

# Where to store the small JSON state file (optional)
# STATE_FILE=/var/tmp/imap_sync_state.json

# Optional: Only sync messages after this date (format: YYYY-MM-DD)
# Uncomment and set to filter messages by date
# AFTER_DATE=2024-01-01
